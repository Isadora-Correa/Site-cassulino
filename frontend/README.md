# Frontend

O frontend principal deste projeto esta no diretorio raiz, usando Vite + React (`src/`).

Esta pasta foi criada para refletir a estrutura solicitada no enunciado do trabalho. Se quiser, em uma segunda etapa podemos mover a aplicacao inteira para dentro daqui.
